// src/modules/{{#if parentPath}}{{parentPath}}/{{/if}}{{kebabCase moduleName}}/infrastructure/{{kebabCase moduleName}}.repository.ts
import { Injectable } from '@nestjs/common';
import { InjectTransaction } from '@nestjs-cls/transactional';
import type { Transaction } from '@nestjs-cls/transactional';
import type { TransactionalAdapterPrisma } from '@nestjs-cls/transactional-adapter-prisma';
import { {{pascalCase moduleName}}, {{pascalCase moduleName}}NotFoundException } from '../domain';
import type { {{pascalCase moduleName}}RepositoryPort } from '../ports/out/{{kebabCase moduleName}}.repository.port';
{{#if withMapper}}
import { {{pascalCase moduleName}}Mapper } from './{{kebabCase moduleName}}.mapper';
{{/if}}

@Injectable()
export class {{pascalCase moduleName}}Repository implements {{pascalCase moduleName}}RepositoryPort {
  constructor(
    @InjectTransaction()
    private readonly tx: Transaction<TransactionalAdapterPrisma>,
    {{#if withMapper}}
    private readonly mapper: {{pascalCase moduleName}}Mapper,
    {{/if}}
  ) {}

  async create(params: {
    // TODO: 파라미터 정의
  }): Promise<{{pascalCase moduleName}}> {
    // TODO: 구현
    throw new Error('Not implemented');
  }

  async findById(id: string): Promise<{{pascalCase moduleName}} | null> {
    // TODO: 구현
    return null;
  }

  async getById(id: string): Promise<{{pascalCase moduleName}}> {
    const entity = await this.findById(id);
    if (!entity) {
      throw new {{pascalCase moduleName}}NotFoundException(id);
    }
    return entity;
  }
}

