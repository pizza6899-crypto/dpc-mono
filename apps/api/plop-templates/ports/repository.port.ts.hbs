// src/modules/{{#if parentPath}}{{parentPath}}/{{/if}}{{kebabCase moduleName}}/ports/out/{{kebabCase moduleName}}.repository.port.ts
import type { {{pascalCase moduleName}} } from '../../domain';

export interface {{pascalCase moduleName}}RepositoryPort {
  /**
   * {{pascalCase moduleName}} 생성
   */
  create(params: {
    // TODO: 파라미터 정의
  }): Promise<{{pascalCase moduleName}}>;

  /**
   * ID로 {{pascalCase moduleName}} 조회
   */
  findById(id: string): Promise<{{pascalCase moduleName}} | null>;

  /**
   * ID로 {{pascalCase moduleName}} 조회 (없으면 예외 발생)
   */
  getById(id: string): Promise<{{pascalCase moduleName}}>;
}

