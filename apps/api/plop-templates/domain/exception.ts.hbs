// src/modules/{{#if parentPath}}{{parentPath}}/{{/if}}{{kebabCase moduleName}}/domain/{{kebabCase moduleName}}.exception.ts

/**
 * {{pascalCase moduleName}} 도메인 예외 기본 클래스
 */
export class {{pascalCase moduleName}}Exception extends Error {
  constructor(message: string) {
    super(message);
    this.name = '{{pascalCase moduleName}}Exception';
  }
}

/**
 * {{pascalCase moduleName}}를 찾을 수 없을 때 발생하는 예외
 */
export class {{pascalCase moduleName}}NotFoundException extends {{pascalCase moduleName}}Exception {
  constructor(id: string) {
    super(`{{pascalCase moduleName}} not found: ${id}`);
    this.name = '{{pascalCase moduleName}}NotFoundException';
  }
}

