// src/modules/{{#if parentPath}}{{parentPath}}/{{/if}}{{kebabCase moduleName}}/domain/{{kebabCase moduleName}}-policy.ts

/**
 * {{pascalCase moduleName}} 도메인 정책 (Policy)
 * 비즈니스 규칙과 검증 로직을 담당
 */
export class {{pascalCase moduleName}}Policy {
  // 비즈니스 규칙 메서드들을 여기에 추가
}

