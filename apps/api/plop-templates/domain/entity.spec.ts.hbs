// src/modules/{{modulePath}}/domain/model/{{kebabCase entityName}}.entity.spec.ts
import { {{pascalCase entityName}} } from './{{kebabCase entityName}}.entity';

describe('{{pascalCase entityName}} Entity', () => {
  describe('fromPersistence', () => {
    it('should create entity from persistence data', () => {
      const data = {
        id: 'test-id',
        createdAt: new Date(),
        updatedAt: new Date(),
      };

      const entity = {{pascalCase entityName}}.fromPersistence(data);

      expect(entity).toBeInstanceOf({{pascalCase entityName}});
      expect(entity.id).toBe(data.id);
      expect(entity.createdAt).toBe(data.createdAt);
      expect(entity.updatedAt).toBe(data.updatedAt);
    });
  });

  describe('toPersistence', () => {
    it('should convert entity to persistence data', () => {
      const data = {
        id: 'test-id',
        createdAt: new Date(),
        updatedAt: new Date(),
      };

      const entity = {{pascalCase entityName}}.fromPersistence(data);
      const persistence = entity.toPersistence();

      expect(persistence).toEqual(data);
    });
  });
});

