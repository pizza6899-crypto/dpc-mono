// src/modules/{{modulePath}}/application/{{kebabCase serviceName}}.service.ts
import { Inject, Injectable } from '@nestjs/common';
// TODO: 필요한 import 추가
// import { Transactional } from '@nestjs-cls/transactional';
// import { EntityName } from '../domain';
// import { ENTITY_REPOSITORY } from '../ports/out/entity.repository.token';
// import type { EntityRepositoryPort } from '../ports/out/entity.repository.port';

interface {{pascalCase serviceName}}Params {
  // TODO: 파라미터 정의
}

interface {{pascalCase serviceName}}Result {
  // TODO: 결과 타입 정의
}

@Injectable()
export class {{pascalCase serviceName}}Service {
  constructor(
    // TODO: 의존성 주입
    // @Inject(ENTITY_REPOSITORY)
    // private readonly repository: EntityRepositoryPort,
  ) {}

  // @Transactional() // 필요시 주석 해제
  async execute(
    params: {{pascalCase serviceName}}Params,
  ): Promise<{{pascalCase serviceName}}Result> {
    // TODO: 구현
    throw new Error('Not implemented');
  }
}

