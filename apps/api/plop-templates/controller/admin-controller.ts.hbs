// src/modules/{{modulePath}}/controllers/admin/{{kebabCase controllerName}}.controller.ts
import { Controller, Get, Post, Body, HttpCode, HttpStatus } from '@nestjs/common';
import { ApiTags, ApiOperation } from '@nestjs/swagger';
import { RequireRoles } from 'src/platform/auth/decorators/roles.decorator';
import { UserRoleType } from 'src/generated/prisma';
import {
  ApiStandardResponse,
  ApiStandardErrors,
} from 'src/platform/http/decorators/api-response.decorator';
import { CurrentUser } from 'src/platform/auth/decorators/current-user.decorator';
import type { CurrentUserWithSession } from 'src/platform/auth/decorators/current-user.decorator';
import { RequestClientInfoParam } from 'src/platform/auth/decorators/request-info.decorator';
import type { RequestClientInfo } from 'src/platform/http/types/client-info.types';
// TODO: Service import 추가
// import { ServiceName } from '../../application/service-name.service';

@Controller('admin/{{kebabCase controllerName}}')
@ApiTags('Admin {{pascalCase controllerName}} (관리자 {{pascalCase controllerName}})')
@ApiStandardErrors()
@RequireRoles(UserRoleType.ADMIN, UserRoleType.SUPER_ADMIN)
export class {{pascalCase controllerName}}Controller {
  constructor(
    // TODO: Service 주입
  ) {}

  @Get()
  @HttpCode(HttpStatus.OK)
  @ApiOperation({
    summary: 'Get {{pascalCase controllerName}}',
    description: '{{pascalCase controllerName}} 조회',
  })
  @ApiStandardResponse(Object, {
    status: HttpStatus.OK,
    description: 'Success',
  })
  async get{{pascalCase controllerName}}(
    @CurrentUser() admin: CurrentUserWithSession,
    @RequestClientInfoParam() requestInfo: RequestClientInfo,
  ) {
    // TODO: 구현
    return {};
  }
}

