{
  "name": "dpc-mono",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "format": "turbo run format",
    "api:dev": "pnpm -F api dev",
    "api:build": "pnpm -F api build",
    "api:start": "pnpm -F api start",
    "api:test": "pnpm -F api test",
    "api:test:dev": "pnpm -F api test:dev",
    "api:lint": "pnpm -F api lint",
    "api:format": "pnpm -F api format",
    "api:cli": "pnpm -F api cli",
    "api:zip": "pnpm -F api zip",
    "api:template": "pnpm -F api template:generate",
    "api:swagger": "pnpm -F api swagger:generate",
    "admin:build": "pnpm -F admin build",
    "admin:start": "pnpm -F admin start",
    "admin:dev": "pnpm -F admin dev",
    "admin:generate": "pnpm -F admin generate",
    "db:generate": "pnpm -F @repo/database generate",
    "db:migrate": "pnpm -F @repo/database migrate",
    "db:format": "pnpm -F @repo/database format",
    "db:reset": "pnpm -F @repo/database reset",
    "db:deploy": "pnpm -F @repo/database deploy",
    "db:seed": "pnpm -F @repo/database seed",
    "db:studio": "pnpm -F @repo/database studio",
    "db:create:unified-logs-view": "pnpm -F @repo/database create:unified-logs-view",
    "db:partition:audit-tables": "pnpm -F @repo/database partition:audit-tables",
    "shared:build": "pnpm -F @repo/shared build",
    "shared:dev": "pnpm -F @repo/shared dev",
    "package:deploy": "node scripts/package-for-deploy.js && open ."
  },
  "devDependencies": {
    "@types/node": "^25.0.3",
    "dotenv-cli": "^11.0.0",
    "turbo": "latest"
  },
  "packageManager": "pnpm@10.26.2"
}